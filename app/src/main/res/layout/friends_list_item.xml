<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tool="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="5dp">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/friend_pic"
        android:layout_width="80dp"
        android:layout_height="80dp"
        tool:src="@mipmap/ic_launcher" />

    <TextView
        android:id="@+id/friend_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingEnd="5dp"
        android:paddingStart="5dp"
        android:textAppearance="@style/TextAppearance.AppCompat.Medium"
        app:layout_constraintBottom_toTopOf="@id/friend_phone"
        app:layout_constraintStart_toEndOf="@id/friend_pic"
        app:layout_constraintTop_toTopOf="parent"
        tool:text="Friend Name" />

    <TextView
        android:id="@+id/friend_phone"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingEnd="5dp"
        android:paddingStart="5dp"
        android:textAppearance="@style/TextAppearance.AppCompat.Medium"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@id/friend_pic"
        app:layout_constraintTop_toBottomOf="@id/friend_name"
        tool:text="Friend Phone Number" />
</android.support.constraint.ConstraintLayout>